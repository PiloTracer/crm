import React from "react";
import { Box, Typography } from "@mui/material";

const Home = () => {
    return (
        <Box>
            <Typography>
                Welcome! Please select a menu option on the left
            </Typography>
        </Box>
    );
};
export default Home;